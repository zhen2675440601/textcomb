# TextComb（文梳）项目指导

本文件是 TextComb 仓库的通用 Codex 开发规范。它随仓库提交，适用于整个项目；更深层目录中的 `AGENTS.md` 可以补充或收紧对应目录的规则。

## 项目目标与边界

- TextComb 面向中国大陆简体中文文章，首版提供错字、标点、基础病句和疑似分段问题的辅助识别。
- 输入支持 UTF-8/GB18030 TXT、文字型 DOCX 和文字型 PDF；不实现 OCR，不处理扫描版、加密或损坏 PDF。
- 输出问题报告，不直接改写原文；报告必须保留可回到原文的位置信息。
- 用户最终自行审核建议。不要把任意模型配置宣称为经过质量验证的参考配置。
- 首版不扩展事实核查、论文格式、全文一致性、专业术语保证、支付、小程序和自动改写。

## 任务与进度同步

- GitHub Issue、Project 和 Pull Request 是任务状态的唯一事实源；聊天记录和本地临时文件不是共享项目状态。
- 开始工作前先阅读本文件、相关 Issue、`README.md` 和适用的 `docs/` 文档，并检查 `git status`、当前分支和最近提交。
- 一个明确功能使用一个 Issue、一个工作分支和一个 Pull Request。若任务很小，也要在提交或 PR 中说明范围和验收结果。
- 开始任务时在 Issue 中说明负责范围；遇到阻塞时立即记录阻塞原因、已尝试方案和需要的决策。
- 完成任务时更新 Issue 清单和状态，在 PR 中写明修改内容、验证命令、测试结果、风险和遗留问题。
- 不要同时在多台主机上修改同一个工作分支。开始前同步 `main`，完成后尽快推送；不要改写已共享分支历史。
- 推荐分支命名：`feat/<topic>`、`fix/<topic>`、`docs/<topic>`、`chore/<topic>`、`test/<topic>`。
- `main` 只通过 Pull Request 合并，除非维护者明确要求，不直接提交到 `main`。

## 架构约束

- 使用模块化单体：Rust API、Rust Worker、管理 CLI、共享领域/核心库和 Vue 3 前端。
- 任务队列使用 PostgreSQL；不要为普通任务流程引入 Redis 或微服务拆分。
- API、Worker 和 CLI 的领域类型优先放在 `crates/textcomb-domain`；跨模块逻辑放在 `crates/textcomb-core`。
- 文档解析只负责提取正文、来源映射和拒绝不支持的输入；分析副本只能规范换行和空白，不能破坏错字定位。
- AI 流程保持候选分析、服务器定位、统一复核、合并去重和报告生成的边界。不要信任模型给出的绝对偏移或依据 ID。
- 服务器必须验证结构化输出、证据白名单、置信度分层和问题位置；确定性代码不得伪装成语言规则判定引擎。
- `ReportV1` 和 OpenAPI 是公共契约。修改公共类型、数据库字段或导出结构时，必须同步更新迁移、文档、前端生成类型和测试。
- 数据库迁移只前滚；破坏性变更至少跨一个版本，并提供兼容或回滚方案。

## 目录职责

- `apps/textcomb-api/`：Axum API、认证、权限、HTTP 错误和 REST/SSE 接口。
- `apps/textcomb-worker/`：任务领取、租约、心跳、分析流水线、重试、取消和清理任务。
- `apps/textcomb-cli/`：迁移、首个超管、用户管理、诊断等管理命令。
- `crates/textcomb-domain/`：ReportV1、问题、位置、状态和领域枚举等公共类型。
- `crates/textcomb-core/`：持久化、文档解析、模型 Provider、分析、报告、加密和队列实现。
- `web/`：Vue 3、TypeScript、Vite 前端；API 类型从 OpenAPI 生成，不手工维护重复接口类型。
- `migrations/`：PostgreSQL 前滚迁移；不要直接修改已应用迁移。
- `tools/`：假模型、评测和开发辅助工具；假模型不得用于真实质量结论。
- `tests/fixtures/`：可再分发的测试样例；不得放入真实用户文章或私有评测集。
- `docs/`：架构、开发、运维、评测和商业路线文档。

## Rust 与前端开发规则

- 按 `rust-toolchain.toml` 使用固定 Rust 工具链和 Edition；保持 `Cargo.lock` 提交。
- Rust 代码必须通过 rustfmt、Clippy 零警告和工作区测试；优先使用类型安全和明确错误，不使用无理由的 `unwrap` 或静默降级。
- SQLx 查询、迁移和领域类型必须保持一致；不得绕过资源所有者校验。
- 前端使用仓库锁定的 Node 版本和 `npm ci`；API 变更后重新生成 `web/src/api/schema.d.ts` 并提交生成结果。
- 复杂逻辑先补测试再实现或至少与实现同时提交；解析、位置映射、切块、去重、权限和删除策略必须有回归测试。
- 注释解释约束、原因和安全边界，不重复代码表面含义。

## AI Provider 与隐私

- 首版只依赖统一的 OpenAI 兼容 Provider 接口；新增原生协议必须先扩展抽象并补测试。
- 模型密钥只能通过加密配置和运行环境主密钥使用；日志、错误、审计事件和导出中不得出现密钥。
- 日志不得记录用户原文、完整提示词、问题片段、密码或第三方模型密钥；只记录请求 ID、用户 ID、任务 ID、阶段、耗时、用量和错误码。
- 向第三方模型发送正文前必须有用户确认；不要把真实文章、私有评测集、商业词典或无授权依据提交到仓库。
- 任何模型输出都必须经过 JSON 结构、原文引用唯一定位、证据白名单和权限边界验证。

## 必做验证

提交代码前，根据改动范围运行至少相关的检查；完整提交前运行：

```shell
cargo fmt --all -- --check
cargo clippy --workspace --all-targets --locked -- -D warnings
cargo test --workspace --all-targets --locked
cd web
npm ci --ignore-scripts
npm run build
```

API、数据库或报告契约变更还要运行对应集成测试，并确认 OpenAPI 生成文件没有未提交差异。真实 AI 评测不在普通 PR 中运行，按 `docs/evaluation.md` 的私有评测流程执行。

## 完成定义

任务只有在以下条件都满足时才算完成：

1. 实现与任务范围一致，没有顺手扩大产品边界。
2. 相关单元测试、集成测试或前端检查已补充并通过。
3. 必要的迁移、生成类型、文档和变更日志已同步。
4. 没有新增密钥、真实用户原文、私有评测数据或无授权内容。
5. Issue、PR 和进度状态已更新，明确记录已知限制。

## Git 提交与 Pull Request

- 提交信息使用简短、明确的英文前缀，例如 `feat: add docx position mapping`、`fix: reject encrypted pdf`、`docs: clarify local setup`。
- 一个提交尽量只表达一个可回滚的逻辑变化；不要提交构建产物、`target/`、`node_modules/`、`.env` 或本机配置。
- PR 标题应说明用户可感知的变化；正文至少包含：关联 Issue、改动摘要、测试命令、数据/安全影响和未完成事项。
- 发现无关的已有修改时保留它们，不要使用破坏性 Git 命令覆盖用户工作。

## Codex 工作方式

- 开始修改前先给出简短执行计划；发现需求、架构或权限存在实质歧义时先停下并说明阻塞点。
- 只修改任务需要的文件；编辑后检查 `git diff`，确认没有意外格式化或越界修改。
- 优先使用仓库已有脚本和文档命令；不要为了通过检查删除测试、放宽安全校验或隐藏错误。
- 完成后报告：修改文件、验证命令及结果、未运行的检查、风险和下一步建议。
- 若用户纠正了重复出现的项目规则，应把可复用的规则补充到本文件或最近的目录级 `AGENTS.md`。

## Code Review Rules

- 重点检查权限越界、会话和密码安全、密钥/正文泄漏、原文位置错误、任务重复执行、取消后仍调用模型、报告串号和不完整报告伪装成功。
- 检查导出 JSON、Markdown、PDF 是否来自同一规范化报告对象且内容一致。
- 检查输入大小、ZIP 炸弹、PDF 解析超时、路径穿越和资源所有者校验是否仍然有效。
- 格式化、Clippy、TypeScript 和普通单元测试等机械检查交给 CI；Review 规则关注会造成数据、隐私或产品行为风险的变化。
